import { ComingSoon } from '@components';
import { Metadata, NextPage } from 'next';

export const metadata: Metadata = {
  title: 'Download | Open Dev Net',
};

const Download: NextPage = () => <ComingSoon />;

export default Download;
