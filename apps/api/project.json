{
  "name": "api",
  "$schema": "https://github.com/nrwl/nx/raw/master/packages/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "apps/api/src",
  "targets": {
    "build": {
      "executor": "nx-rust:build",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/api"
      },
      "configurations": {
        "production": {
          "release": true
        }
      }
    },
    "test": {
      "executor": "nx-rust:test",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/api"
      },
      "configurations": {
        "production": {
          "release": true
        }
      }
    },
    "lint": {
      "executor": "nx-rust:lint",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/api"
      }
    },
    "format": {
      "executor": "nx-rust:format",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/api"
      }
    },
    "install": {
      "executor": "nx-rust:install",
      "outputs": ["{options.path}"],
      "options": {
        "path": "apps/api"
      }
    },
    "dev": {
      "executor": "nx-rust:run",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/test"
      },
      "configurations": {
        "production": {
          "release": true
        }
      }
    }
  },
  "tags": []
}
