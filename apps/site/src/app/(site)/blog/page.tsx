import { ComingSoon } from '@components';
import { Metadata, NextPage } from 'next';

export const metadata: Metadata = {
  title: 'Blog | Open Dev Net',
};

const Blog: NextPage = () => <ComingSoon />;

export default Blog;
